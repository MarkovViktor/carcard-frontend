import './App.css';
import Header from "../Header/Header";

function App() {

  // function a() {

  // }

  return (
    <div className="App">
      <Header></Header>
        {/* <Switch>
          <Route exact path="/">
            <Main />
          </Route>

          {loggedIn && (
            <ProtectedRoute
            />
          )}

          {loggedIn && (
            <ProtectedRoute
            />
          )}

          {loggedIn && (
            <ProtectedRoute
            />
          )}
          <Route path="*">
            <NotFound />
          </Route>
        </Switch> */}
    </div>
  );
}

export default App;
